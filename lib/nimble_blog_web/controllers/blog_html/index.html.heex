<h2 class="">All <%= @filter && @filter || nil %> blog posts: </h2>

<%= for post <- @posts do %>
  <div id={post.id} class="post-item" style="margin-bottom: 3rem;">
    <p><%= post.author %>, <%= post.date |> Blog.get_time_from_now() %>:</p>

    <%= raw(post.description) %>

    <h3>
      <.link patch={"/post/#{post.id}"}>
        <img src={post.illustration}  alt={post.illustration_alt} class="w-full mx-auto my-8" />
        <span class="font-pixel-hair text-xl font-bold">|></span> <%= post.title %>
      </.link>
    </h3>

    <p id="post-tags">
      Tagged as
      <%= for tag <- post.tags do %>
        <.link patch={"/tag/#{tag}"} class="text-ultra-violet font-bold"><%= tag %></.link>
      <% end %>
      - <%= post.body |> Blog.get_reading_duration() %> min read
    </p>
  </div>
<% end %>
